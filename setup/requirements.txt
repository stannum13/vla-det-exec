# PyTorch (torch==2.1.2, torchvision==0.16.2) is installed by setup/setup.sh
# via the PyTorch CUDA 12.1 index. Run setup.sh before pip install -r requirements.txt.
numpy>=1.24
pandas>=2.0
h5py>=3.9
tqdm>=4.65
pytest>=7.4
einops>=0.7
huggingface_hub>=0.20
